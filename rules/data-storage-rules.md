When need for persistant server-side data storage, always:

- use the useStorage composable from Nuxt to store it in key value storage unless otherwise declared
- wrap storage logic into a seperate service
